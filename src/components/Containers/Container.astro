---
const { class: customClass } = Astro.props;
---

<div class={`container p-2 md:p-4 xl:p-6 max-w-2xl mx-auto ${customClass}`}>
	<slot />
</div>

<style>
	.container {
		max-width: var(--container-8xl);

		&.full {
			max-width: none;
		}

		&.wide {
			max-width: var(--container-5xl);
		}
	}
</style>
