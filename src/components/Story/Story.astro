---
import Iframe from "@src/components/Iframe/Iframe.astro";
import H2 from "@src/components/Typography/H2.astro";
import P from "@src/components/Typography/P.astro";

interface Props {
	dark?: boolean;
	subtitle?: string;
	url?: string;
}

const { dark = false, url, subtitle } = Astro.props;
---

<div
	class={`grid grid-cols-1 md:grid-cols-2 gap-5 mx-auto max-w-2xl py-3 ${dark ? "border-custom-corn" : "border-gray-400"} border-y-1	 mb-8`}
>
	<div class="">
		<H2 class="!mb-0 ml-0" dark={dark}>Relato sonoro</H2>
		<P class="mb-3 !text-xs" dark={dark}>{subtitle}</P>
		<P class="mb-3" dark={dark}>
			<slot />
		</P>
	</div>
	<div class="flex items-center">
		<Iframe url={url} class="!mb-0" />
	</div>
</div>
